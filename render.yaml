services:
  - type: web
    name: inhoustonagentes
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn api.main:app --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:10000
